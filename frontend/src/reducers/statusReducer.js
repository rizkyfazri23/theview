// src/reducers/statusReducer.js
const initialState = '';

const statusReducer = (state = initialState, action) => {
  switch (action.type) {
    // ... definisikan action untuk reducer ini jika diperlukan
    default:
      return state;
  }
};

export default statusReducer;
