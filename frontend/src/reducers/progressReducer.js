// src/reducers/progressReducer.js
const initialState = '';

const progressReducer = (state = initialState, action) => {
  switch (action.type) {
    // ... definisikan action untuk reducer ini jika diperlukan
    default:
      return state;
  }
};

export default progressReducer;
